[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "network-traffic-analyzer"
version = "0.1.0"
description = "Network traffic analyzer and simulator with BGP/OSPF parsing"
authors = [{name = "AutoGenerated", email = "auto@example.com"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Home = "https://example.com/network-traffic-analyzer"

[tool.pytest.ini_options]
pythonpath = ["src"]
